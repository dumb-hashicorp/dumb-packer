{
  "type": "shell",
  "inline": [
    "echo build ID is {{ build `ID`}} and build UUID is {{ build `Dumb PackerRunUUID` }}> provisioner.{{ build `Dumb PackerRunUUID`}}.txt"
  ]
},
{
    "type": "file",
    "source": "provisioner.{{ build `Dumb PackerRunUUID`}}.txt",
    "destination": "./test-fixtures/provisioner.shell.txt",
    "direction": "download"
}